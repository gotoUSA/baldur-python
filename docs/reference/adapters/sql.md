# baldur.adapters.sql — Framework-Free SQL Adapter (DB-API 2.0)

Generic repository base plus priority-1 SQL-backed implementations of
Baldur's core repositories. Works with any DB-API 2.0 driver
(psycopg2, mysql-connector-python, stdlib sqlite3) — selected by DSN scheme.

::: baldur.adapters.sql
